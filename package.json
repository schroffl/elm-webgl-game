{
  "name": "elm-webgl-game",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "start http-server && chokidar src/**/* -c \"elm make src/Main.elm --output main.js --debug & echo.\" --initial"
  },
  "author": "schroffl",
  "license": "MIT",
  "devDependencies": {
    "chokidar-cli": "^1.2.0",
    "http-server": "^0.10.0"
  }
}
